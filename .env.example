# Puerto local del ERP. El core utiliza otro proceso y otro puerto.
PORT=3001

# Core y ERP corren en la misma computadora, en procesos separados.
# No se publica automáticamente al navegador y no debe contener secretos.
NEXT_PUBLIC_API_URL=http://127.0.0.1:3000
